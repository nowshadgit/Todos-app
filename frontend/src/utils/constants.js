const LOGIN_FORM_HEADER = 'Sign In';
const LOGIN_BUTTON_TEXT = 'Sign In';

const SIGNUP_FORM_HEADER = 'Sign up';
const SIGNUP_BUTTON_TEXT = 'Sign up';

const TODOS = 'Todos';
const ADD_TODO = 'Add todo';
const UPDATE_TODO = 'Update todo';
const ADD = 'Add';

const SUBMIT = 'Submit';

const PAGE = {
  LOGIN: 'LOGIN',
  TODOS: 'TODOS',
  ADD_TODO:'ADD_TODO',
  UPDATE_TODO: 'UPDATE_TODO',
  SIGN_UP: 'SIGN_UP'
};

const CALL_STATUS = {
  ERROR: 'error',
  PENDING: 'pending',
  RESPONDED: 'responded',
  IN_PROGRESS: 'in-progress',
  RESEND_ERROR: 'resend-error',
  TOKEN_ERROR: 'TOKEN_ERROR',
  TOKEN_RESPONDED: 'TOKEN_RESPONDED'
};

export {
  PAGE,
  CALL_STATUS,
  SUBMIT,
  LOGIN_FORM_HEADER,
  LOGIN_BUTTON_TEXT,
  SIGNUP_FORM_HEADER,
  SIGNUP_BUTTON_TEXT,
  TODOS,
  ADD_TODO,
  UPDATE_TODO,
  ADD
};
